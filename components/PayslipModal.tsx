import React from 'react';

// Payslip feature has been disabled. Component kept as a no-op to avoid import errors.
const PayslipModal: React.FC<any> = () => {
  return null;
};

export default PayslipModal;
